import React from 'react'
import { Button, extend } from '../../baseComponents'

const PrimaryButton = extend(Button, theme => ({
	// Primary Button Styles
	button: {}
}))

export default PrimaryButton
